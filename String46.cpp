#include pt4.h
using namespace std;

void Solve()
{
    Task(String46);
    string s;
    pts;
    s=s+' ';
    int pos1=s.find(' '),pos0=-1,result=0;

    while(pos1!=-1)
    {
        if(pos1-pos0-1result)
            result=pos1-pos0-1;

        ShowN(result);
        pos0=pos1;
        pos1=s.find(' ',pos1+1);
    }
    ptresult;
}
#include pt4.h
using namespace std;

void Solve()
{
    Task(String46);
    string s;
    pts;
    s=s+' ';
    int pos1=s.find(' '),pos0=-1,result=0;

    while(pos1!=-1)
    {
        if(pos1-pos0-1result)
            result=pos1-pos0-1;

        ShowN(result);
        pos0=pos1;
        pos1=s.find(' ',pos1+1);
    }
    ptresult;
}
